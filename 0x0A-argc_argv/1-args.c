#include <stdio.h>
/**
 * main: Check the code
 * @argv: arrray of string
 * @argc: The number of argument passed in the cmd
 * Return: 0
 */
int main(int argc, char *argv[])
{
        argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
