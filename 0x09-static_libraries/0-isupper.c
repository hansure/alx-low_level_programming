#include "main.h"

/**
 * _isupper: Checking if a character is upper letter
 * x: The number to be checked
 *
 * Return: 1 for upper case letter else 0
 */
int _isupper(int x)
{

	if (x >= 65 && x <= 90)
	{
	
		return (1);
	}

	return (0);
}
