#include "main.h"
/**
 * Absolute value function that prints the absolute value of the number
 * Return always positive number
 */

int _abs(int num)
{
if(num >= 0)
	return (num);
else
	return (-num);
}
